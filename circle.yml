dependencies:
  pre:
    - bower install
  post:
    - npm install module-deps
    - npm install gulp -g

machine:
  environment:
    DB_URL: mongodb://root:abc123@ds045694.mongolab.com:45694/dms
    PORT: 3000
    SECRET_KEY: H46Blh8[01JQM,3<4C]dfO)mT9yKMa
    CODECLIMATE_REPO_TOKEN: 8167ae56d3e5478238f015d158bae278d696085488a2e16142eb1f99f9a1cfbc codeclimate-test-reporter < lcov.info

test:
  override:
    - gulp build
    - npm test

deployment:
  production:
    branch: master
    heroku:
      appname: doc-man
