dependencies:
  pre:
    - bower install
  post:
    - npm install module-deps
    - npm install gulp -g

database:
  override:
    - cp server/config/database.yml.ci server/config/database.yml

machine:
  environment:
    NODE_ENV: test
    CIRCLE_ENV: test
    DATABASE_USERNAME:
    DATABASE_PASSWORD:
    DATABASE_URL: localhost
    DATABASE_NAME: circle_test
    DATABASE_DIALECT: mongodb
    DATABASE_PORT: 27019
    DATABASE_URL: mongodb://ubuntu:@127.0.0.1:27019/circle_test
    PORT: 3000
    SECRET_KEY: H46Blh801JQM3<4CdfOmT9yKMa
    CODECLIMATE_REPO_TOKEN: 8167ae56d3e5478238f015d158bae278d696085488a2e16142eb1f99f9a1cfbc

test:
  override:
    - gulp build
    - npm test

deployment:
  production:
    branch: master
    heroku:
      appname: doc-man
