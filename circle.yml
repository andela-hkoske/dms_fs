dependencies:
  pre:
    - bower install
  post:
    - npm install module-deps
    - npm install gulp -g

machine:
  environment:
    DATABASE_URL: mongodb://localhost/dms
    PORT: 3000
    SECRET_KEY: H46Blh8[01JQM,3<4C]dfO)mT9yKMa

test:
  override:
    - gulp build
    - npm test

deployment:
  production:
    branch: master
    heroku:
      appname: doc-man
